# Baseline mirror for workspace/oracle_owasp_file_upload.yaml.
# Same query, scored against the unified agent's general behavior (no oracle/* RAG
# partitions yet on the unified path). The specialized workspace case in
# workspace/oracle_owasp_file_upload.yaml exercises the suitescript_workspace
# knowledge profile.

query: "What's the right way to handle file uploads in a SuiteScript RESTlet?"
agent_id: unified-agent
tags: ["workspace", "oracle", "owasp", "baseline"]
notes: |
  Unified-agent baseline for the SuiteScript file-upload security question.
  Without the suitescript_workspace profile + oracle RAG, the unified agent
  has no direct access to Oracle's OWASP guidance — this case captures the
  pre-RAG behavior so we can measure delta.
expected_answer_contains:
  - "validat"
expected_tools: []
expected_accuracy: 0.0
max_cost: 0.30
max_latency_ms: 60000
