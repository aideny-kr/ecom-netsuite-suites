# Named-metric ask: net margin last quarter.
#
# D2 "measured" benchmark case — grill R2#19, NEW-4, NEW-4a.
#
# SCOPE — ROUTING-ONLY (NEW-4a): net_margin is a SYSTEM expression metric whose
# suiteql leaves (net_income, gross_revenue) seed as status=draft, so
# compute_metric returns missing_dependency and NO data_table value is produced
# live. This case therefore measures ROUTING (did the agent call
# metric_compute for the net_margin key?), not a real computed value.
#   - computed_value_absent: true and expected_metric_key: net_margin are still
#     REQUIRED and now mean: "must route through metric_compute(net_margin);
#     the value-absent invariant is VACUOUSLY satisfied when no value is
#     produced (there is no value to leak)."
#   - The benchmark gate (run_vs_mcp.py::_classify_metric_compute_routing)
#     de-conflates a genuine BYPASS (metric_compute never called → hard fail)
#     from a correctly-routed-but-uncomputable metric (routing-only PASS) from
#     a WRONG-key call (routing-identity hard fail). An empty metric_data_table
#     no longer auto-fails; the tool_calls log is the discriminator.
#   - When the suiteql leaves are promoted to status=active with blessed
#     queries, net_margin computes a real value and the value-absent invariant
#     is exercised live end-to-end — revisit this "routing-only" note then.
#
# Tests that a named-metric ask routes through metric_compute (the metric
# catalog) rather than falling back to ad-hoc SuiteQL. This is the key
# routing regression: if metric_compute is NOT used, the agent has bypassed
# the catalog and is computing net margin ad-hoc, which diverges from the
# tenant-declared metric definition (formula, period semantics, filter set).
#
# Expected behavior:
#   1. Agent recognises "net margin" as a catalog-defined metric by name.
#   2. Agent calls metric_compute(metric_key="net_margin", period="last_quarter").
#   3. Routing is satisfied by the metric_compute(net_margin) call; a computed
#      value is NOT expected live (draft leaves → missing_dependency).
#   4. Any computed numeric value MUST NOT appear in the model's text answer
#      (it is delivered exclusively via the data_table SSE event). Vacuous
#      while no value is produced.
#
# NEW-4 CI fix: this case is now run as a dedicated CI step via
#   --case metric_net_margin_last_quarter
# (the --suite sales step excludes it because the case_id starts with
# 'metric_', not 'sales_').
#
# Anti-hallucination invariant (computed_value_absent: true):
#   The benchmark runner must extract numeric values from the data_table
#   tool result returned by metric_compute and verify none appear verbatim
#   in the model's answer text, using scorer.assert_computed_value_absent().
#   A leak means the SSE interception was bypassed — hard fail (score = 0.0).
#   See backend/app/services/benchmarks/scorer.py for the helper.
#
# NOTE: This case will fail gracefully when ANTHROPIC_API_KEY is absent
# (benchmark step is skipped); the unit test in test_metric_benchmark_case.py
# guards the case's declared intent independently of a live run.

case_id: metric_net_margin_last_quarter
query: "what's our net margin last quarter?"
tags: ["metric", "named-metric", "catalog", "net-margin", "fiscal-period"]
notes: |
  ROUTING-ONLY named-metric test. The agent must call metric_compute for the
  net_margin catalog metric rather than constructing ad-hoc SuiteQL. net_margin
  is a system expression metric whose draft suiteql leaves yield
  missing_dependency, so NO value is produced live — the gate scores routing,
  and the value-absent invariant is vacuously satisfied when no value exists.
  Acceptable period interpretation: "last quarter" = the most recently
  completed fiscal quarter per tenant fiscal calendar.
  Expected tool: metric_compute (NOT netsuite_suiteql directly).
expected_answer_contains:
  - "net margin"
expected_tools:
  - "metric_compute"
expected_accuracy: 0.7
max_cost: 0.50
max_latency_ms: 120000
# Anti-hallucination invariant (NEW-4): the computed metric value must NOT
# appear in the model's text answer — it belongs in the data_table SSE event.
# The benchmark runner extracts numeric strings from the metric_compute tool
# result and calls scorer.assert_computed_value_absent(). Hard fail (0.0) if
# any value leaks verbatim into the LLM prose.
computed_value_absent: true
# NEW-4a routing-identity: the agent must compute the SPECIFIC net_margin metric
# (not just any metric). The key is matched against the data_table's `query` field
# which metric_compute sets to the metric key.
expected_metric_key: net_margin
# Baseline: Claude+MCP has no metric_compute tool; it would compute net
# margin via ad-hoc SuiteQL (income / revenue). baseline_expected_tools
# is empty because the Oracle MCP suite has no catalog compute tool.
baseline_expected_accuracy: 0.5
baseline_expected_tools: []
