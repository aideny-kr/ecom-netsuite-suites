# Design: Restore NetSuite Knowledge After v2.0 Agent Revamp

Generated by /office-hours on 2026-04-16
Branch: feat/cross-source-sheets
Repo: aideny-kr/ecom-netsuite-suites
Status: APPROVED
Mode: Intrapreneurship

## Problem Statement

After PR #40/#43 ("knowledge-driven unified agent v2.0," shipped 2026-04-15), Framework's chat agent has lost the ability to recall NetSuite-specific patterns it was previously taught. The most reproducible symptom: the agent rediscovers (and fails to rediscover) the `transactionShippingAddress` join pattern on every turn, burns 10+ tool calls per session, and the user cancels the run. Users report "the rules and golden data we taught are lost."

The revamp's architectural decisions were correct — fewer routing seams, model self-routing via tool_use, knowledge profiles for domain context. The regression is a *content* problem, not an *architecture* problem: the new system has the right boxes but the boxes are empty for NetSuite.

## Demand Evidence

Hour-of-staging-logs from 2026-04-16 (Framework, tenant `ce3dfaad-626f-4992-84e9-500c8291ca0a`):

- Two consecutive sessions (`e3e5fd5a-...` and `c2ab437b-...`) both burned 10+ `ns_runCustomSuiteQL` calls trying to discover the shipping-country join. User cancelled the second run.
- `[PATTERN_RETRIEVAL] tenant=ce3dfaad q="..." returned=0` for every query in the hour, including the user's literal request `"can you load the rules for framework sales questions?"`.
- User explicitly typed `"Can you look from NetSuite?"` mid-session — the agent had picked the wrong source on the prior turn and the user had to redirect it.
- Agent emitted `t.status NOT IN ('SalesOrd:C', 'SalesOrd:H')` — the exact compound-status-code bug forbidden by `unified_agent.py:261-263` and CLAUDE.md rule #13. The rule is in the prompt; the agent ignored it.
- `[LEARNED_RULES_RETRIEVAL] total=38` — Framework has 38 learned rules and the always-on slice is being injected, so the retrieval pipeline works in principle. Patterns and domain knowledge are the broken slice.

**DB verification 2026-04-16 (Supabase staging):**
- `tenant_query_patterns` for Framework **contains 6 shipping-country patterns**, all created 2026-04-09 / 2026-04-10 during Olivia's session before auto-learn was disabled. All use `JOIN transactionShippingAddress sa ON sa.nKey = t.shippingAddress` + `BUILTIN.DF(sa.country)`. All `success_count=5`. `last_used_at` on all six is **2026-04-10 00:58** — **not used in the six days since**. Patterns are present; retrieval is failing.
- `tenant_learned_rules` for Framework: 9 active field-guidance rules. **None** mention `transactionShippingAddress`, shipping country, `nKey`, or "don't use custbody for country." The parallel precedent exists for *location* (rule created 2026-03-16 says "use `tl.location`, NOT custom body fields like `custbody_fw_sent_to_panurgy`") but no equivalent rule was ever written for country. The "don't use custbody" guidance for country lived only as the shape of the saved patterns, never as an explicit rule.

This is not a one-off bug report. It is a regression in the trust contract: users were told the system would remember corrections, and on April 16 it does not — the mechanism stored the patterns correctly in April and then stopped using them.

## Status Quo

- **Workaround (live today):** users re-state the rule each turn, watch the agent rediscover the join across multiple tool calls, sometimes cancel the run and rephrase. Not sustainable — every Framework analyst will hit this within their first three queries.
- **Pre-revamp behavior:** specialist agent prompts (60–80 lines each for BI, Pricing, Recon) embedded domain expertise directly. NetSuite SuiteQL rules were duplicated across `recon_agent.md` and the unified prompt. Auto-learning persisted user corrections to `tenant_query_patterns` until 2026-04-09 when it was disabled for pattern-pollution reasons.
- **Old escape hatch:** users could "teach" the agent live and the next session would remember. Auto-learn off + no admin-seed pipeline = nothing replaced this loop.

## Target User & Narrowest Wedge

**Target:** Framework ops/finance users (Olivia and team) who ask SuiteQL-flavored sales/inventory/AR questions in chat. Internal analysts who already trust the agent for BigQuery work and now expect parity for NetSuite.

**Narrowest wedge:** Within one chat session, when a Framework user asks "show me sales by shipping country," the agent generates the correct SuiteQL on the first attempt — including the `transactionShippingAddress` join with `sa.nKey = t.shippingAddress`, single-letter status codes, and the standard `transactionline` revenue filters. No rediscovery loop. No cancellation.

## Constraints

- **No regression on v2.0 architecture.** Don't reintroduce specialist agents, three-tier routing, or veto phrases. The revamp deleted 6,600 lines for cause; this work must operate inside the knowledge-profile model.
- **No re-enabling auto-learning from live sessions.** Auto-learn was disabled 2026-04-09 because of pattern pollution. Replace with admin-seed + (later) eval-gated nightly promotion, not by re-enabling the live loop.
- **Must preserve the source-pin and disambiguation behaviors that already work.** The fix to `prompt_assembler.py` is additive: honor explicit user source naming, otherwise current behavior stands.
- **Must follow the existing knowledge-profile pattern.** No new abstractions. `netsuite.yaml` looks like `bigquery.yaml`.
- **Domain knowledge ingest must use the existing `ingest_domain_knowledge.py` script and `domain_knowledge_chunks` table.** No new pipeline.
- **No production secret changes; no Alembic migrations needed for Phase 1 or Phase 2** (existing tables suffice).

## Premises

1. The user's report is accurate AND under-states the scope — but the DB check reframes *which* structure is broken. Framework's 6 shipping-country patterns are still present in `tenant_query_patterns` (verified 2026-04-16 via Supabase). Retrieval — not storage — is the bleeding wound. Additionally: (a) no learned rule for country exists alongside the patterns (parallel to the existing location rule), (b) `golden_dataset/` files are orphaned from RAG, (c) no `netsuite.yaml` profile exists, (d) no admin-seed pipeline exists. Fixing retrieval alone restores Olivia's April 10 behavior; fixing the structural gaps prevents future losses.
2. The fix is split into two waves to mirror the HITL+v2.0 release pattern: a same-day hotfix to restore user trust, then a structural fix this week to make it durable. This prevents "perfect-is-the-enemy-of-shipped."
3. SuiteQL dialect rules currently live in two places (the unified base prompt and effectively nowhere else, since specialist prompts were deleted). They should be moved into `netsuite.yaml`'s `prompt_fragment` so they only inject when NetSuite tools are connected. This shrinks the base prompt for non-NS tenants and removes the duplication risk CLAUDE.md rule #24 warns about.
4. Pattern retrieval must always run when SuiteQL or BigQuery tools are available. The current `_need_patterns` gate in `orchestrator.py:1387` ties patterns to `ContextNeed.DATA` only — `FULL` (investigation queries) deliberately skips patterns by the matrix design at `orchestrator.py:1374-1383`. That decision was made when patterns were noisy and live-learned. Now that patterns will be admin-seeded and high-quality, the gate should change: patterns retrieve whenever a NetSuite or BigQuery query tool is present, regardless of `ContextNeed`. Trade: marginally larger context window on FULL queries; benefit: seeded patterns can never be stranded by a context classifier choice.
5. Re-emphasizing in-prompt rules (status codes, joins) is necessary but not sufficient. The April 16 evidence shows the agent ignored a rule that's literally in the prompt. The durable fix is worked examples in `tenant_query_patterns` (high-similarity retrieval, hard to ignore) plus golden dataset chunks (RAG, surfaces in context window with precedent).

## Approaches Considered

### Approach A: Surgical hotfix only (rejected as standalone)
Insert correct shipping-country pattern into Framework's `tenant_query_patterns` via direct SQL. Update disambiguation prompt. Done.
- **Effort:** S (~1-2 hours)
- **Risk:** Low for the immediate fix; high for the next regression.
- **Pros:** Restores user trust today; minimal blast radius.
- **Cons:** Doesn't address the structural gap. The next pattern users teach will rot the same way. Doesn't ingest the orphaned golden dataset.
- **Reuses:** `tenant_query_patterns`, `prompt_assembler.py`.

### Approach B: Full structural rebuild only (rejected as standalone)
Add `netsuite.yaml`, ingest 14 golden_dataset files into RAG, build admin-seed script, audit all Framework patterns and learned_rules. Move SuiteQL rules out of the base prompt.
- **Effort:** L (~1 day, possibly more)
- **Risk:** Medium — moving rules out of the base prompt risks breaking non-Framework tenants if a tool other than `netsuite_suiteql` triggers SuiteQL writes.
- **Pros:** Fully durable; the next "taught" rule survives a revamp.
- **Cons:** Doesn't ship today. User keeps hitting the rediscovery loop in the meantime.
- **Reuses:** Knowledge profile loader, `ingest_domain_knowledge.py`, `domain_knowledge_chunks`.

### Approach C: Hybrid — A today, B this week (CHOSEN)
Same-day hotfix restores user trust. Structural fix this week makes it durable. Two PRs, one this morning and one over the next 1-2 days.
- **Effort:** S (today) + M (this week).
- **Risk:** Low; staged. Phase 1 is reversible. Phase 2 builds on a now-quiet baseline.
- **Pros:** User stops bleeding today. Structural fix carries the longer-term load. Mirrors the HITL+v2.0 two-wave shipping pattern that worked yesterday.
- **Cons:** Two PRs instead of one. Phase 1 inserts SQL by hand (no script yet) — that's a one-time tradeoff, not a permanent shortcut.
- **Reuses:** Everything Approach A and Approach B reuse.

## Recommended Approach

**Approach C: Hybrid.** Restore trust this morning; harden the system this week.

### Phase 1 — Today (~2-3 hours)

**1. Diagnose why retrieval returned 0 despite 6 patterns being present.**

Skip seeding — the 6 shipping-country patterns already exist in `tenant_query_patterns` (verified 2026-04-16 via DB query). The question is why `[PATTERN_RETRIEVAL] returned=0` in the live session. Two likely causes, to be tested in order:

(a) **Gating failed to request patterns at all.** Phase 1 step 3 (below) fixes this at the source.

(b) **Similarity below threshold.** User's question *"4 new countries we recently launched"* may embed too far from seeded questions like *"sales data by shipping country"* or *"sales breakdown by country with laptop and desktop quantities"*. `PATTERN_MIN_SIMILARITY` is 0.45 per CLAUDE.md. Write a diagnostic script that:
1. Embeds the user's question via `_embed_text`.
2. Pulls the 6 seeded patterns' `intent_embedding` from DB.
3. Computes cosine similarity for each.
4. Prints the top match and confirms whether it clears 0.45.

If the top score is < 0.45: either lower `PATTERN_MIN_SIMILARITY` modestly (0.40?) or add 1-2 additional patterns with broader phrasings closer to how users actually ask ("countries launched recently," "new markets," "shipping destinations"). Prefer adding patterns over lowering threshold (threshold change is global blast radius; new patterns are tenant-scoped).

If the top score is ≥ 0.45: the gate is the culprit (see step 3 below), not the similarity. No new patterns needed.

**2. Add a Framework learned rule for shipping country (mirrors the location rule precedent).**

Insert one `term_definition` rule (always-on category, not query-gated) via `tenant_save_learned_rule` tool or direct DB insert. Wording:

> "For sales/order queries filtered or grouped by shipping country, ALWAYS use `JOIN transactionShippingAddress sa ON sa.nKey = t.shippingAddress` and read `BUILTIN.DF(sa.country)` for the country name (or `sa.country` for the 2-letter ISO code). Do NOT use custom body fields like `custbodyfw_ship_country_ext` — the shipping country is a standard NetSuite field on the shipping address, not a custom field. The `transactionShippingAddress` join key is `sa.nKey = t.shippingAddress`, NOT `sa.recordOwner = t.id` or `sa.transaction = t.id`."

This is the exact parallel to the location rule from 2026-03-16. `term_definition` category keeps it always-injected regardless of query keywords, which is what we want — it's universally relevant whenever any NetSuite query touches country.

**3. Update `prompt_assembler.py:9-20` `DISAMBIGUATION_INSTRUCTION`.**
Add an explicit-naming clause as the FIRST rule (before "use the most authoritative one") so it has unambiguous precedence:
> "PRECEDENCE: If the user explicitly names a source ('in NetSuite', 'from BigQuery', 'check NetSuite for ...'), use ONLY that source. Do not call any other source unless the user explicitly asks for both ('compare NetSuite to BigQuery', 'and also BigQuery'). The explicit-naming rule overrides the both-source rule."

Keep the rest of the existing instruction intact below this clause.

**4. Replace the `_need_patterns` gate in `orchestrator.py:1387` with tool-presence detection.**
Today: `_need_patterns = context_need in (ContextNeed.DATA,)`. The `FULL` (investigation) path deliberately skips patterns by the matrix design at `orchestrator.py:1374-1383` — that design assumed patterns were noisy live-learned content. With admin-seeded high-quality patterns, the gate should change.

Decision: patterns retrieve whenever `netsuite_suiteql` or `bigquery_sql` (or any `ext__*__ns_runCustomSuiteQL`) tool is in the available tool set, regardless of `ContextNeed`. Concretely (using existing `_tool_names` set computed at `orchestrator.py:1142`):
```python
_need_patterns = bool({"netsuite_suiteql", "bigquery_sql"} & _tool_names) or any(
    name.startswith("ext__") and "suiteql" in name.lower() for name in _tool_names
)
```
Trade-off: marginally larger context window on FULL queries (~1-2K tokens for top-K patterns). Benefit: seeded patterns can never be stranded by a context classifier choice. Document the change in the orchestrator's injection-matrix comment so the matrix at lines 1374-1383 stays honest.

**5. Smoke-test on staging.**
Re-run the broken query ("4 new countries we recently launched"). Expected outcome: agent generates correct SuiteQL on the first attempt, no rediscovery loop. Pattern retrieval logs should show `returned≥1` with a matched pattern citing `sa.nKey = t.shippingAddress`.

**Phase 1 deliverable:** one PR with: (a) the disambiguation prompt change, (b) the `_need_patterns` gate replacement, (c) the new country learned rule (as a data insert documented in the PR, OR via a one-off runbook script). Pattern-similarity diagnostic is a runbook step (separate from PR).

### Phase 2 — This week (~1 day)

**1. `seed_tenant_patterns.py` admin script.**
Input: YAML file of `{question, working_sql, tenant_id, notes}` records. Reuses `query_pattern_service._embed_text()` for embeddings. Upserts into `tenant_query_patterns` via SQLAlchemy `pg_insert(...).on_conflict_do_update(constraint="uq_tenant_query_pattern", ...)` against the existing `(tenant_id, working_sql)` constraint (per `query_pattern_service.py:163`).

Mirror the structure of `ingest_domain_knowledge.py`. Place under `backend/scripts/`. CLI usage:
```
.venv/bin/python -m scripts.seed_tenant_patterns --tenant ce3dfaad-... --file knowledge/seeded_patterns/framework_netsuite.yaml
```

**2. `netsuite.yaml` knowledge profile.**
File: `backend/app/services/chat/knowledge_profiles/netsuite.yaml`
- `trigger_tools`: ALL NetSuite read-side tools — `netsuite_suiteql`, `netsuite_financial_report`, `ns_runReport`, `ns_runSavedSearch`, `ext__*__ns_runCustomSuiteQL`, `ext__*__ns_getSuiteQLMetadata`, `ext__*__ns_getRecord`, `ext__*__ns_runReport`, `ext__*__ns_runSavedSearch`, `ext__*__ns_getSavedSearchSchema`. Reason: SuiteQL rules apply when ANY NetSuite read tool is present, because saved-search and financial-report sessions can spill into ad-hoc SuiteQL on follow-up turns. Write-side stays in `netsuite_writes.yaml`.
- `prompt_fragment`: Move the 150 lines of SuiteQL dialect rules from `unified_agent.py:173-313` into here. Keep the same wording verbatim per CLAUDE.md rule #24. Add a worked `transactionShippingAddress` example.
- `rag_partitions`: `netsuite/suiteql-rules`, `netsuite/joins`, `netsuite/transactions`, `netsuite/multi-currency`, `netsuite/record-types`.

**Risk mitigation:** Before deleting the rules from `unified_agent.py`, confirm no tenant has zero NetSuite tools but still asks SuiteQL questions (CLAUDE.md rule #28 says one unified agent — a tenant with only BigQuery + Pricing tools should not be writing SuiteQL). Add a regression test that asserts `netsuite.yaml.prompt_fragment` contains the same dialect rules verbatim after the move.

**3. Ingest golden dataset into RAG.**
Of the 14 files in `knowledge/golden_dataset/`, 11 are NetSuite-specific and 3 are BigQuery (the latter belong under `bi/*` partitions referenced by `bigquery.yaml`, not in this PR's scope unless they're verified missing from `bi/*` already). NetSuite mapping:

| Partition | Files |
|---|---|
| `netsuite/suiteql-rules` | `suiteql-syntax-rules.md`, `suiteql-example-queries.md`, `common-errors-and-recovery.md`, `date-and-time-patterns.md` |
| `netsuite/joins` | `join-patterns-and-aggregation.md`, `transaction-relationships.md` |
| `netsuite/transactions` | `transaction-types-and-statuses.md`, `financial-statements.md` |
| `netsuite/multi-currency` | `multi-currency-rules.md` |
| `netsuite/record-types` | `record-types-and-columns.md`, `custom-fields-and-records.md` |

BigQuery files (`bigquery-bi-analytics.md`, `bigquery-data-transformation.md`, `bigquery-sql-patterns.md`) — verify they're already in `bi/schema-docs` or `bi/common-queries`; if not, ingest them as a side task and update `bigquery.yaml` partitions accordingly. Out of scope for this PR if they're already there.

Run `ingest_domain_knowledge.py` per file with the matching partition. Verify retrieval similarity for representative queries (target ≥ 0.55).

**4. Add `transactionShippingAddress` worked example.**
Append a clearly labeled worked example to the END of `knowledge/golden_dataset/join-patterns-and-aggregation.md` covering the exact pattern from commit `6ef4fc4`. Append-to-end keeps `chunk_index` stable for existing chunks (avoids orphaning prior chunks in `domain_knowledge_chunks` since `ingest_domain_knowledge.py` upserts on `uq_dk_source_chunk` keyed by source+chunk_index). Re-ingest that file.

**5. Audit `_INVESTIGATION_RE` and pattern gating end-to-end.**
After Phase 1's narrow gating fix, do a fuller pass: ensure context-need classification routes Framework-style sales queries to FULL with patterns + DK + learned_rules together.

**Phase 2 deliverable:** one PR with `seed_tenant_patterns.py`, `netsuite.yaml`, base-prompt slim-down (move rules out), worked example file, and a CHANGELOG note explaining the architecture.

## Open Questions

1. **Are there OTHER patterns Framework "forgot"?** Partially resolved by DB check: 6 shipping-country patterns present, all `last_used_at = 2026-04-10`. Remaining unknowns: (a) how many other patterns exist across all categories — a full `SELECT user_question, last_used_at FROM tenant_query_patterns WHERE tenant_id = 'ce3dfaad...'` dump, (b) which ones haven't retrieved since April 10 (the auto-learn disable boundary), (c) whether any of those are also "forgotten rules" from the user's perspective. Action: dump full pattern inventory during Phase 1 diagnostic.
2. **Do we need to gate the `netsuite.yaml` profile by connector health?** Today, `cross_source.yaml` triggers when both BQ and NS tools are present. `netsuite.yaml` should trigger when NS tools are present regardless of BQ. Verify the loader's `matches_tools()` does this correctly with no edge cases.
3. **Should the prompt-rule attention failure (`SalesOrd:C` despite the rule being in the prompt) get its own ticket?** This is a separate, broader issue: the agent ignored a rule that was right there. Fix idea: surface the rule with worked examples (which Phase 2 partly addresses via patterns), or move the rule earlier in the prompt closer to the tool definition. Track separately.
4. **Pattern admission threshold for nightly auto-improvement.** Once Phase 2 ships, the autonomous-improvement nightly job can promote eval-validated patterns. What's the minimum eval pass rate to promote? Defer to autonomous-improvement skill.
5. **Non-Framework tenants and the global Phase 1 disambiguation change.** The disambiguation prompt change is global — it lands for every tenant whose session has 2+ data-source profiles active. Verify no tenant currently relies on the agent calling both sources when one is named (e.g., a workflow that says "in NetSuite" but expects BigQuery context too). Action: spot-check active staging tenants before merging Phase 1; if any do, the precedence clause needs softening to "prefer the named source" rather than "use ONLY."

## Success Criteria

- **Phase 1 success (today):** Re-running the broken query on staging — "what are the 4 new countries we recently launched?" — produces correct SuiteQL on the first attempt with `sa.nKey = t.shippingAddress` join, no `recordOwner=t.id` or `transaction=t.id` misfires, no `custbodyfw_ship_country_ext` misfires, AND no compound status codes (`'SalesOrd:C'`). Pattern retrieval logs show `returned≥1` for the matched pattern (one of the 6 April-10 patterns). Learned rules logs show the new country rule in the always-on slice. `last_used_at` on the matched pattern updates post-turn. User confirms via a fresh staging session. If `'SalesOrd:C'` still recurs despite pattern + rule + disambiguation fixes, escalate — the prompt-attention gap (Open Question #3) is then a real blocker, not a deferred concern.
- **Phase 2 success (this week):** Three independent staging tests:
  - "Show sales by shipping country since Jan 1" → first-attempt correct SQL.
  - "Compare net revenue Q1 vs Q2 by subsidiary" → uses correct multi-currency rules from RAG.
  - "Pivot order counts by month and country" → uses pivot tool, correct join.
  All three should pass without the agent rediscovering joins or status codes.
- **Quantitative:** vs-MCP benchmark wins on the existing 18-case sales suite either holds or improves. CI gate stays green.

## Distribution Plan

Existing pipeline. Phase 1 PR → CI → staging deploy via `saas-deployment` skill. Phase 2 PR → same path. No new infrastructure, no new secrets, no Alembic migrations.

For the Phase 1 country learned-rule insert: use the existing `tenant_save_learned_rule` tool (via a chat session as Framework admin) OR a one-off `.venv/bin/python` insert into `tenant_learned_rules`. Document the rule verbatim in the Phase 1 PR description so it's reproducible. Phase 2 adds `seed_tenant_patterns.py` for bulk pattern operations — it should also support bulk learned-rule seeding, or a sibling `seed_tenant_learned_rules.py` script, so future "teach the agent" operations can be scripted.

## Dependencies

- Existing `tenant_query_patterns` table (migration 034).
- Existing `domain_knowledge_chunks` table.
- Existing embedding service in `query_pattern_service.py`.
- `ingest_domain_knowledge.py` script.
- Knowledge profile loader and `prompt_assembler.py`.
- No external dependencies. No new packages. No new env vars.

## The Assignment

Before opening the Phase 1 PR: open a fresh staging session as Framework, ask "what are the 4 new countries we recently launched?", and screenshot the assistant's first SuiteQL attempt (will show `recordOwner=t.id` or similar wrong join, or the custbody_ fallback). Save the screenshot. After Phase 1 ships, ask the same question again and screenshot — expected: first-attempt `sa.nKey = t.shippingAddress` with `BUILTIN.DF(sa.country)`. The before/after image is the single best artifact for explaining to the team why this fix mattered — and the "trust restored" moment for any other user who saw the bad behavior. Two minutes of work, permanent record of the regression and the fix.

## What I noticed about how you think

- You said *"It didn't ask me and went on for bigquery first."* That's a precise behavioral observation, not a vibe — you noticed the agent silently chose a source instead of asking. Most users would say "it gave me the wrong answer." You named the *failure mode*. That's how you debug systems other people would just complain about.
- When I offered four ways to attack the fix (A/B/C/D), you picked D — "open the staging transcript first." You didn't take the recommended option. You wanted evidence before committing to a plan. The transcript completely changed the diagnosis (BigQuery wasn't the live failure, pattern starvation was). Your instinct saved the design from a wrong-target fix.
- You called out the right structural concern unprompted: "rules and golden data we taught are lost." You weren't reporting a single bug; you were reporting a *trust contract* break. That framing is what made me look at patterns + golden dataset together instead of just one of them.
- You shipped PR #40 the day before this regression surfaced and you're already open about diagnosing what it left behind. Most people would defend the revamp. You're treating it as a starting point, not a finish line.
