"""Extraction-shaped desktop orchestration seed (rich-pipe slice 1).

Imports nothing from Electron/IPC/transport so the later `packages/agent`
extraction relocates it unchanged. See `README.md` in this package.
"""
